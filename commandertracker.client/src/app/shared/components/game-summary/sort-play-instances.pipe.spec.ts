import { SortPlayInstancesPipe } from './sort-play-instances.pipe';

describe('SortPlayInstancesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPlayInstancesPipe();
    expect(pipe).toBeTruthy();
  });
});
