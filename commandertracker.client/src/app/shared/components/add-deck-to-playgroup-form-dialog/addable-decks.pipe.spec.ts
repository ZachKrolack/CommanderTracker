import { AddableDecksPipe } from './addable-decks.pipe';

describe('AddableDecksPipe', () => {
  it('create an instance', () => {
    const pipe = new AddableDecksPipe();
    expect(pipe).toBeTruthy();
  });
});
