import { IsColorActivePipe } from './is-color-active.pipe';

describe('IsColorActivePipe', () => {
    it('create an instance', () => {
        const pipe = new IsColorActivePipe();
        expect(pipe).toBeTruthy();
    });
});
