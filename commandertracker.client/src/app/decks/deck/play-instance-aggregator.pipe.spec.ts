import { PlayInstanceAggregatorPipe } from './play-instance-aggregator.pipe';

describe('PlayInstanceAggregatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayInstanceAggregatorPipe();
    expect(pipe).toBeTruthy();
  });
});
